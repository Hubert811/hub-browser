diff --git a/packages/browseros-agent/apps/claw-server-rust/src/api/http/harness.rs b/packages/browseros-agent/apps/claw-server-rust/src/api/http/harness.rs
new file mode 100644
index 000000000..1f1458b30
--- /dev/null
+++ b/packages/browseros-agent/apps/claw-server-rust/src/api/http/harness.rs
@@ -0,0 +1,481 @@
+//! Harness reporting surface: an out-of-process harness (hub) that drives the
+//! browser through its OWN CDP connection can still feed this server's audit
+//! trail — session lifecycle, tool dispatches, and tab-ownership windows — so
+//! its sessions appear in the cockpit timeline with attributed recordings.
+//!
+//! The trust model matches the rest of the local API surface: the server binds
+//! localhost only, and these routes append audit rows rather than executing
+//! browser actions, so an untrusted local caller gains no browser control.
+//! Rows are append-only; the audit projections (task summary, session list)
+//! are refreshed exactly the way the MCP dispatch path refreshes them.
+
+use super::{error, internal};
+use crate::{
+    AppState,
+    clock::now_epoch_ms,
+    db::audit_log::{RecordToolDispatchInput, bounded_args_json},
+    error::{CanonicalError, RequestId},
+    ids::{DispatchId, SessionId},
+};
+use axum::{
+    Extension, Json,
+    body::Body,
+    extract::{Path, Query, State},
+    http::StatusCode,
+    response::Response,
+};
+use claw_api::models::{
+    HarnessClaimTabResponse, HarnessDispatchResponse, HarnessReleaseTabResponse,
+    HarnessSessionEndResponse, HarnessSessionStartResponse, RecordingStreamEntry,
+    RecordingStreamList,
+};
+use serde::Deserialize;
+use serde_json::Value;
+use tracing::warn;
+use ulid::Ulid;
+
+const MAX_AGENT_FIELD_LEN: usize = 128;
+const DEFAULT_STREAM_LIMIT: i64 = 200;
+const MAX_STREAM_LIMIT: i64 = 1_000;
+
+#[derive(Debug, Deserialize)]
+#[serde(rename_all = "camelCase")]
+pub(super) struct StartSessionBody {
+    /// Client-supplied stable session id (e.g. a hub conversation id). A fresh
+    /// Ulid is minted when absent, letting lightweight callers stay id-agnostic.
+    #[serde(default)]
+    session_id: Option<String>,
+    /// Stable harness identity, e.g. "hub". Free-form: the audit tables key
+    /// sessions by id, not by a closed agent enum.
+    #[serde(default = "default_agent_id")]
+    agent_id: String,
+    /// Cockpit-facing slug, e.g. "hub".
+    #[serde(default = "default_slug")]
+    slug: String,
+    /// Human label, e.g. "hub-browser".
+    #[serde(default = "default_agent_label")]
+    agent_label: String,
+    #[serde(default = "default_client_name")]
+    client_name: String,
+    #[serde(default)]
+    client_version: String,
+}
+
+fn default_agent_id() -> String {
+    "hub".to_string()
+}
+fn default_slug() -> String {
+    "hub".to_string()
+}
+fn default_agent_label() -> String {
+    "hub-browser".to_string()
+}
+fn default_client_name() -> String {
+    "hub".to_string()
+}
+
+#[derive(Debug, Deserialize)]
+#[serde(rename_all = "camelCase")]
+pub(super) struct EndSessionBody {
+    /// Free-form lifecycle kind, e.g. "normal" / "idle".
+    #[serde(default = "default_end_kind")]
+    kind: String,
+    #[serde(default)]
+    reason: Option<String>,
+}
+
+fn default_end_kind() -> String {
+    "normal".to_string()
+}
+
+#[derive(Debug, Deserialize)]
+#[serde(rename_all = "camelCase")]
+pub(super) struct DispatchBody {
+    tool_name: String,
+    /// Optional page/tab attribution; the fields flow straight into the
+    /// dispatch row so the cockpit timeline and replay attribution work.
+    #[serde(default)]
+    page_id: Option<i64>,
+    #[serde(default)]
+    tab_id: Option<i64>,
+    #[serde(default)]
+    target_id: Option<String>,
+    #[serde(default)]
+    url: Option<String>,
+    #[serde(default)]
+    title: Option<String>,
+    /// Tool arguments; bounded to the same audit width as the MCP path.
+    #[serde(default)]
+    args: Option<Value>,
+    /// Short result summary, e.g. "ok" or "error: not_found".
+    #[serde(default = "default_result_meta")]
+    result_meta: String,
+    #[serde(default)]
+    duration_ms: Option<i64>,
+    /// Epoch-ms start for long-running tools so timeline ordering matches
+    /// execution order; defaults to write time.
+    #[serde(default)]
+    created_at: Option<i64>,
+    /// Client-side dispatch id (Ulid-shaped); minted when absent.
+    #[serde(default)]
+    dispatch_id: Option<String>,
+    #[serde(default)]
+    parent_dispatch_id: Option<String>,
+    #[serde(default)]
+    agent_id: Option<String>,
+    #[serde(default)]
+    slug: Option<String>,
+    #[serde(default)]
+    agent_label: Option<String>,
+}
+
+fn default_result_meta() -> String {
+    "ok".to_string()
+}
+
+#[derive(Debug, Deserialize)]
+#[serde(rename_all = "camelCase")]
+pub(super) struct ClaimTabBody {
+    tab_id: i64,
+    #[serde(default)]
+    target_id: Option<String>,
+    /// Epoch-ms claim start; defaults to now.
+    #[serde(default)]
+    claimed_at: Option<i64>,
+    #[serde(default)]
+    agent_id: Option<String>,
+}
+
+pub(super) async fn start_session(
+    Extension(request_id): Extension<RequestId>,
+    State(state): State<AppState>,
+    Json(body): Json<StartSessionBody>,
+) -> Result<Json<HarnessSessionStartResponse>, CanonicalError> {
+    let session_id = match body.session_id {
+        Some(id) if is_ulidish(&id) => id,
+        Some(_) => {
+            return Err(error(
+                &request_id,
+                StatusCode::BAD_REQUEST,
+                "invalid_request",
+                "sessionId must be a client-supplied Ulid-shaped identifier",
+            ));
+        }
+        None => SessionId::new(Ulid::new().to_string()).into_inner(),
+    };
+    let agent_id = bounded_field(&body.agent_id);
+    let slug = bounded_field(&body.slug);
+    let agent_label = bounded_field(&body.agent_label);
+    state
+        .audit_log
+        .record_session_start(
+            &session_id,
+            &agent_id,
+            &slug,
+            &agent_label,
+            &bounded_field(&body.client_name),
+            &bounded_field(&body.client_version),
+        )
+        .await
+        .map_err(|source| internal(&request_id, source))?;
+    Ok(Json(HarnessSessionStartResponse::new(session_id)))
+}
+
+pub(super) async fn end_session(
+    Extension(request_id): Extension<RequestId>,
+    State(state): State<AppState>,
+    Path(session_id): Path<String>,
+    Json(body): Json<EndSessionBody>,
+) -> Result<Json<HarnessSessionEndResponse>, CanonicalError> {
+    ensure_known_session(&state, &request_id, &session_id).await?;
+    state
+        .audit_log
+        .record_session_end(
+            &session_id,
+            &bounded_field(&body.kind),
+            body.reason.as_deref(),
+        )
+        .await
+        .map_err(|source| internal(&request_id, source))?;
+    // Close every open tab-ownership window so replay attribution and orphan
+    // cleanup see the same end state the MCP teardown path produces.
+    state
+        .session_tabs
+        .release_claims_for_session(&session_id)
+        .await
+        .map_err(|source| internal(&request_id, source))?;
+    Ok(Json(HarnessSessionEndResponse::new(true)))
+}
+
+pub(super) async fn report_dispatch(
+    Extension(request_id): Extension<RequestId>,
+    State(state): State<AppState>,
+    Path(session_id): Path<String>,
+    Json(body): Json<DispatchBody>,
+) -> Result<Json<HarnessDispatchResponse>, CanonicalError> {
+    ensure_known_session(&state, &request_id, &session_id).await?;
+    let tool_name = bounded_field(&body.tool_name);
+    if tool_name.is_empty() {
+        return Err(error(
+            &request_id,
+            StatusCode::BAD_REQUEST,
+            "invalid_request",
+            "toolName must not be empty",
+        ));
+    }
+    let input = RecordToolDispatchInput {
+        agent_id: bounded_field(body.agent_id.as_deref().unwrap_or("hub")),
+        slug: bounded_field(body.slug.as_deref().unwrap_or("hub")),
+        agent_label: bounded_field(body.agent_label.as_deref().unwrap_or("hub-browser")),
+        session_id: session_id.clone(),
+        tool_name,
+        page_id: body.page_id,
+        tab_id: body.tab_id,
+        target_id: body.target_id,
+        url: body.url,
+        title: body.title,
+        args_json: bounded_args_json(body.args.as_ref().unwrap_or(&Value::Null)),
+        result_meta: truncate(&body.result_meta, 512),
+        duration_ms: body.duration_ms.unwrap_or(0).max(0),
+        created_at: body.created_at.filter(|at| *at > 0),
+        dispatch_id: match body.dispatch_id {
+            Some(id) if is_ulidish(&id) => DispatchId::from(id),
+            _ => DispatchId::new(),
+        },
+        parent_dispatch_id: body
+            .parent_dispatch_id
+            .filter(|id| is_ulidish(id))
+            .map(DispatchId::from),
+        // External harness rows carry no token accounting; version 0 marks
+        // unmeasured rows in the estimator contract.
+        tool_input_token_estimate: 0,
+        tool_output_token_estimate: 0,
+        token_estimator_version: 0,
+    };
+    let row_id = state
+        .audit_log
+        .record_tool_dispatch(input)
+        .await
+        .map_err(|source| internal(&request_id, source))?;
+    // Tab-attributed rows get a server-side preview screenshot, exactly like
+    // the MCP path's audit observer produces for native sessions: the same
+    // SessionVisualService pipeline (claim-window candidate, viewport JPEG,
+    // in-flight concurrency guards) and the same screenshots store + marker.
+    // Fire-and-forget so the report response stays as cheap as before; rows
+    // without a claimable tab capture nothing (select_candidate -> None).
+    if body.tab_id.is_some() {
+        let state = state.clone();
+        let session_id = session_id.clone();
+        tokio::spawn(async move {
+            persist_harness_screenshot(&state, &session_id, row_id).await;
+        });
+    }
+    Ok(Json(HarnessDispatchResponse::new(row_id)))
+}
+
+/// Attaches a viewport screenshot to a harness dispatch row — the native
+/// pipeline (visuals capture -> screenshots store -> mark_screenshot) re-run
+/// for sessions that hold no live MCP `Session`. Failures are logged and
+/// never fail the report; captures of unowned/closed tabs simply skip.
+async fn persist_harness_screenshot(state: &AppState, session_id: &str, row_id: i64) {
+    let bytes = match state.visuals.capture_by_session_id(session_id).await {
+        Ok(Some(bytes)) => bytes,
+        Ok(None) => return,
+        Err(error) => {
+            warn!(error = %error, row_id, "harness screenshot capture failed");
+            return;
+        }
+    };
+    if let Err(error) = state.screenshots.write(session_id, row_id, &bytes).await {
+        warn!(error = %error, row_id, "harness screenshot write failed");
+        return;
+    }
+    if let Err(error) = state.audit_log.mark_screenshot(row_id).await {
+        warn!(error = %error, row_id, "harness screenshot marker failed");
+    }
+}
+
+pub(super) async fn claim_tab(
+    Extension(request_id): Extension<RequestId>,
+    State(state): State<AppState>,
+    Path(session_id): Path<String>,
+    Json(body): Json<ClaimTabBody>,
+) -> Result<Json<HarnessClaimTabResponse>, CanonicalError> {
+    ensure_known_session(&state, &request_id, &session_id).await?;
+    if body.tab_id <= 0 {
+        return Err(error(
+            &request_id,
+            StatusCode::BAD_REQUEST,
+            "invalid_request",
+            "tabId must be a positive Chrome tab id",
+        ));
+    }
+    let agent_id = bounded_field(body.agent_id.as_deref().unwrap_or("hub"));
+    let claimed_at = body
+        .claimed_at
+        .filter(|at| *at > 0)
+        .unwrap_or_else(now_epoch_ms);
+    state.session_tabs.enqueue_claim_tab_for_session(
+        body.tab_id,
+        body.target_id,
+        session_id,
+        agent_id,
+        claimed_at,
+    );
+    // The enqueue path is the production writer; drain turns this request
+    // into a synchronous confirmation for the caller.
+    state.session_tabs.drain_writes().await;
+    Ok(Json(HarnessClaimTabResponse::new(true)))
+}
+
+pub(super) async fn release_tab(
+    Extension(request_id): Extension<RequestId>,
+    State(state): State<AppState>,
+    Path((session_id, tab_id)): Path<(String, i64)>,
+) -> Result<Json<HarnessReleaseTabResponse>, CanonicalError> {
+    ensure_known_session(&state, &request_id, &session_id).await?;
+    if tab_id <= 0 {
+        return Err(error(
+            &request_id,
+            StatusCode::BAD_REQUEST,
+            "invalid_request",
+            "tabId must be a positive Chrome tab id",
+        ));
+    }
+    state
+        .session_tabs
+        .enqueue_release_tab_for_session(tab_id, session_id);
+    state.session_tabs.drain_writes().await;
+    Ok(Json(HarnessReleaseTabResponse::new(true)))
+}
+
+async fn ensure_known_session(
+    state: &AppState,
+    request_id: &RequestId,
+    session_id: &str,
+) -> Result<(), CanonicalError> {
+    let known = state
+        .audit_log
+        .get_task_summary(session_id)
+        .await
+        .map_err(|source| internal(request_id, source))?
+        .is_some();
+    if known {
+        return Ok(());
+    }
+    Err(error(
+        request_id,
+        StatusCode::NOT_FOUND,
+        "not_found",
+        "unknown session; start it with POST /api/v1/harness/sessions first",
+    ))
+}
+
+fn is_ulidish(value: &str) -> bool {
+    let len = value.len();
+    (26..=64).contains(&len) && value.chars().all(|c| c.is_ascii_alphanumeric() || c == '-')
+}
+
+fn bounded_field(value: &str) -> String {
+    truncate(value.trim(), MAX_AGENT_FIELD_LEN)
+}
+
+fn truncate(value: &str, max: usize) -> String {
+    if value.len() <= max {
+        return value.to_string();
+    }
+    let mut cut = max;
+    while cut > 0 && !value.is_char_boundary(cut) {
+        cut -= 1;
+    }
+    value[..cut].to_string()
+}
+
+/// Lists recorded streams for discovery: the out-of-process harness cannot
+/// know which Chrome tabs captured rrweb data, so it starts from this index.
+#[derive(Debug, Deserialize, Default)]
+#[serde(rename_all = "camelCase")]
+pub(super) struct StreamListQuery {
+    from_ms: Option<i64>,
+    to_ms: Option<i64>,
+    #[serde(default)]
+    tab_id: Option<i64>,
+    limit: Option<i64>,
+}
+
+pub(super) async fn list_streams(
+    Extension(request_id): Extension<RequestId>,
+    State(state): State<AppState>,
+    Query(query): Query<StreamListQuery>,
+) -> Result<Json<RecordingStreamList>, CanonicalError> {
+    let limit = query
+        .limit
+        .unwrap_or(DEFAULT_STREAM_LIMIT)
+        .clamp(1, MAX_STREAM_LIMIT);
+    let rows = state
+        .recordings
+        .list_streams(query.from_ms, query.to_ms, query.tab_id, limit)
+        .await
+        .map_err(|source| internal(&request_id, source))?;
+    let streams = rows
+        .into_iter()
+        .map(|row| {
+            let mut entry = RecordingStreamEntry::new(
+                row.document_id,
+                row.tab_id,
+                row.first_event_at,
+                row.last_event_at,
+                row.size_bytes,
+                row.event_count,
+                row.has_gap,
+            );
+            entry.target_id = row.target_id;
+            entry
+        })
+        .collect();
+    Ok(Json(RecordingStreamList::new(streams)))
+}
+
+#[derive(Debug, Deserialize, Default)]
+#[serde(rename_all = "camelCase")]
+pub(super) struct StreamEventsQuery {
+    from_ms: Option<i64>,
+    to_ms: Option<i64>,
+}
+
+pub(super) async fn stream_events(
+    Extension(request_id): Extension<RequestId>,
+    State(state): State<AppState>,
+    Path(document_id): Path<String>,
+    Query(query): Query<StreamEventsQuery>,
+) -> Result<Response, CanonicalError> {
+    if document_id.is_empty() || document_id.len() > 128 {
+        return Err(error(
+            &request_id,
+            StatusCode::BAD_REQUEST,
+            "invalid_request",
+            "documentId must be a non-empty recording document id",
+        ));
+    }
+    let from = query.from_ms.filter(|at| *at > 0).unwrap_or(i64::MIN);
+    let to = query.to_ms.filter(|at| *at > 0).unwrap_or(i64::MAX);
+    let events = state
+        .recordings
+        .read_range(&document_id, from, to)
+        .await
+        .map_err(|source| internal(&request_id, source))?;
+    let mut ndjson = String::new();
+    for event in events {
+        ndjson.push_str(
+            &serde_json::to_string(&event)
+                .map_err(|source| internal(&request_id, source.into()))?,
+        );
+        ndjson.push('\n');
+    }
+    let mut response = Response::new(Body::from(ndjson));
+    response.headers_mut().insert(
+        axum::http::header::CONTENT_TYPE,
+        axum::http::HeaderValue::from_static("application/x-ndjson"),
+    );
+    Ok(response)
+}
diff --git a/packages/browseros-agent/apps/claw-server-rust/src/api/http/mod.rs b/packages/browseros-agent/apps/claw-server-rust/src/api/http/mod.rs
index c89aa1445..d43dee242 100644
--- a/packages/browseros-agent/apps/claw-server-rust/src/api/http/mod.rs
+++ b/packages/browseros-agent/apps/claw-server-rust/src/api/http/mod.rs
@@ -20,6 +20,7 @@ use ulid::Ulid;
 pub(crate) mod audit;
 mod cockpit;
 mod connections;
+mod harness;
 mod live;
 mod previews;
 mod recordings;
@@ -82,6 +83,28 @@ pub fn router(state: AppState) -> Router<AppState> {
             post(recordings::append_document_events)
                 .layer(DefaultBodyLimit::max(RECORDING_INGEST_MAX_BYTES)),
         )
+        .route("/api/v1/recordings/streams", get(harness::list_streams))
+        .route(
+            "/api/v1/recordings/streams/{document_id}/events",
+            get(harness::stream_events),
+        )
+        .route("/api/v1/harness/sessions", post(harness::start_session))
+        .route(
+            "/api/v1/harness/sessions/{session_id}/end",
+            post(harness::end_session),
+        )
+        .route(
+            "/api/v1/harness/sessions/{session_id}/dispatches",
+            post(harness::report_dispatch),
+        )
+        .route(
+            "/api/v1/harness/sessions/{session_id}/tabs",
+            post(harness::claim_tab),
+        )
+        .route(
+            "/api/v1/harness/sessions/{session_id}/tabs/{tab_id}/release",
+            post(harness::release_tab),
+        )
         .route("/api/v1/connections", get(connections::list))
         .route(
             "/api/v1/connections/{harness}",
diff --git a/packages/browseros-agent/apps/claw-server-rust/src/db/recording_index.rs b/packages/browseros-agent/apps/claw-server-rust/src/db/recording_index.rs
index cf178be71..ff4cbcddc 100644
--- a/packages/browseros-agent/apps/claw-server-rust/src/db/recording_index.rs
+++ b/packages/browseros-agent/apps/claw-server-rust/src/db/recording_index.rs
@@ -132,6 +132,19 @@ impl From<tab_claims::Model> for LegacyClaimRow {
     }
 }
 
+/// One recording stream for the discovery listing (see `list_streams`).
+#[derive(Debug, Clone, FromQueryResult)]
+pub struct StreamListRow {
+    pub document_id: String,
+    pub tab_id: i64,
+    pub target_id: Option<String>,
+    pub first_event_at: i64,
+    pub last_event_at: i64,
+    pub size_bytes: i64,
+    pub event_count: i64,
+    pub has_gap: bool,
+}
+
 #[derive(Debug, Clone, FromQueryResult)]
 pub struct StreamMatchRow {
     pub document_id: String,
@@ -416,6 +429,34 @@ impl RecordingIndex {
 
     /// Every live recording stream's document id, for the replay-file orphan
     /// sweep to distinguish tracked files from drift left by a crashed writer.
+    /// Newest-first index of recorded streams, optionally bounded to a time
+    /// window and/or one Chrome tab. The discovery surface for out-of-process
+    /// harnesses that cannot see the claim table.
+    pub async fn list_streams(
+        &self,
+        from_ms: Option<i64>,
+        to_ms: Option<i64>,
+        tab_id: Option<i64>,
+        limit: i64,
+    ) -> AppResult<Vec<StreamListRow>> {
+        let mut query =
+            RecordingStreams::find().order_by_desc(recording_streams::Column::LastEventAt);
+        if let Some(from) = from_ms {
+            query = query.filter(recording_streams::Column::LastEventAt.gte(from));
+        }
+        if let Some(to) = to_ms {
+            query = query.filter(recording_streams::Column::FirstEventAt.lte(to));
+        }
+        if let Some(tab) = tab_id {
+            query = query.filter(recording_streams::Column::TabId.eq(tab));
+        }
+        Ok(query
+            .limit(limit.max(1) as u64)
+            .into_model::<StreamListRow>()
+            .all(self.db.connection())
+            .await?)
+    }
+
     pub async fn all_document_ids(&self) -> AppResult<Vec<String>> {
         Ok(RecordingStreams::find()
             .select_only()
diff --git a/packages/browseros-agent/apps/claw-server-rust/src/services/cockpit/visual.rs b/packages/browseros-agent/apps/claw-server-rust/src/services/cockpit/visual.rs
index b4b75fc10..ba447a4fe 100644
--- a/packages/browseros-agent/apps/claw-server-rust/src/services/cockpit/visual.rs
+++ b/packages/browseros-agent/apps/claw-server-rust/src/services/cockpit/visual.rs
@@ -84,20 +84,27 @@ impl SessionVisualService {
         let Some(session) = self.sessions.lookup(&session_key).await else {
             return Ok(None);
         };
-        self.capture_with_session(&session, true).await
+        self.capture_with_session(session.id().as_str(), true).await
     }
 
     /// Captures through a teardown-owned session lease after live request resolution has stopped.
     pub async fn capture_for_session(&self, session: &Arc<Session>) -> AppResult<Option<Vec<u8>>> {
-        self.capture_with_session(session, false).await
+        self.capture_with_session(session.id().as_str(), false).await
+    }
+
+    /// Captures by bare session id — for harness sessions (out-of-process
+    /// harness reporting) that exist in the audit trail and tab-claim ledger
+    /// but hold no live MCP `Session` object. Same pipeline and in-flight
+    /// guards as live captures; never requires the session to stay live.
+    pub async fn capture_by_session_id(&self, session_id: &str) -> AppResult<Option<Vec<u8>>> {
+        self.capture_with_session(session_id, false).await
     }
 
     async fn capture_with_session(
         &self,
-        session: &Arc<Session>,
+        session_id: &str,
         require_live_at_end: bool,
     ) -> AppResult<Option<Vec<u8>>> {
-        let session_id = session.id().as_str();
         self.session_tabs.drain_writes().await;
         let browser = match self.browser.session().await {
             Some(browser) => browser,
diff --git a/packages/browseros-agent/apps/claw-server-rust/src/services/recordings/store.rs b/packages/browseros-agent/apps/claw-server-rust/src/services/recordings/store.rs
index b09266c25..b5f557c6d 100644
--- a/packages/browseros-agent/apps/claw-server-rust/src/services/recordings/store.rs
+++ b/packages/browseros-agent/apps/claw-server-rust/src/services/recordings/store.rs
@@ -282,6 +282,18 @@ impl RecordingStore {
             .join(format!("{key}.ndjson"))
     }
 
+    /// Discovery listing of recorded streams (newest first); see
+    /// `RecordingIndex::list_streams`.
+    pub async fn list_streams(
+        &self,
+        from_ms: Option<i64>,
+        to_ms: Option<i64>,
+        tab_id: Option<i64>,
+        limit: i64,
+    ) -> AppResult<Vec<crate::db::recording_index::StreamListRow>> {
+        self.index.list_streams(from_ms, to_ms, tab_id, limit).await
+    }
+
     pub async fn read_range(
         &self,
         document_id: &str,
diff --git a/packages/browseros-agent/apps/claw-server-rust/tests/canonical_routes.rs b/packages/browseros-agent/apps/claw-server-rust/tests/canonical_routes.rs
index 053e91f52..84a64adcd 100644
--- a/packages/browseros-agent/apps/claw-server-rust/tests/canonical_routes.rs
+++ b/packages/browseros-agent/apps/claw-server-rust/tests/canonical_routes.rs
@@ -1902,3 +1902,340 @@ async fn audit_cleanup_runs_and_returns_usage() -> anyhow::Result<()> {
     assert!(body["usage"]["totalBytes"].is_number());
     Ok(())
 }
+
+#[tokio::test]
+async fn harness_reporting_surface_end_to_end() -> anyhow::Result<()> {
+    let app = test_app().await?;
+
+    // Start an out-of-process harness session with a client-supplied id.
+    let (status, _, bytes) = request(
+        &app.router,
+        "POST",
+        "/api/v1/harness/sessions",
+        Some("application/json"),
+        Body::from(
+            json!({
+                "sessionId": "01JZZHUBSESSION00000000000000",
+                "agentId": "hub",
+                "slug": "hub",
+                "agentLabel": "hub-browser",
+                "clientName": "hub",
+                "clientVersion": "0.2.0"
+            })
+            .to_string(),
+        ),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    assert_eq!(
+        json_body(&bytes)?,
+        json!({ "sessionId": "01JZZHUBSESSION00000000000000" })
+    );
+
+    // Dispatches on unknown sessions are rejected with the canonical envelope.
+    let (status, _, bytes) = request(
+        &app.router,
+        "POST",
+        "/api/v1/harness/sessions/01JZZUNKNOWNSESSION0000000000/dispatches",
+        Some("application/json"),
+        Body::from(json!({ "toolName": "browser.click" }).to_string()),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::NOT_FOUND);
+    assert_eq!(json_body(&bytes)?["code"], "not_found");
+
+    // Report a dispatch the way hub would after executing one of its tools.
+    let (status, _, bytes) = request(
+        &app.router,
+        "POST",
+        "/api/v1/harness/sessions/01JZZHUBSESSION00000000000000/dispatches",
+        Some("application/json"),
+        Body::from(
+            json!({
+                "toolName": "browser.click",
+                "pageId": 3,
+                "tabId": 101,
+                "targetId": "target-7",
+                "url": "https://example.com/article",
+                "title": "Example",
+                "args": { "ref": "e12" },
+                "resultMeta": "ok",
+                "durationMs": 45
+            })
+            .to_string(),
+        ),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    assert!(json_body(&bytes)?["dispatchId"].is_number());
+
+    // The harness session is now first-class in the cockpit session list.
+    let (status, _, bytes) =
+        request(&app.router, "GET", "/api/v1/sessions", None, Body::empty()).await?;
+    assert_eq!(status, StatusCode::OK);
+    let list = json_body(&bytes)?;
+    let hub_entry = list["items"]
+        .as_array()
+        .and_then(|items| {
+            items
+                .iter()
+                .find(|item| item["sessionId"] == "01JZZHUBSESSION00000000000000")
+        })
+        .cloned();
+    let hub_entry = hub_entry.expect("harness session appears in the session list");
+    assert_eq!(hub_entry["slug"], "hub");
+    assert_eq!(hub_entry["dispatchCount"], 1);
+
+    // Claim the tab the harness is driving.
+    let (status, _, _) = request(
+        &app.router,
+        "POST",
+        "/api/v1/harness/sessions/01JZZHUBSESSION00000000000000/tabs",
+        Some("application/json"),
+        Body::from(json!({ "tabId": 101, "targetId": "target-7", "claimedAt": 100 }).to_string()),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+
+    // Ingest rrweb events for that tab the way the browser extension does.
+    let recording_headers = [
+        ("x-recording-tab-id", "101"),
+        (
+            "x-recording-document-id",
+            "33D25F3CF060E81B14070BC356FF1871",
+        ),
+        ("x-recording-batch-id", "batch-hub-1"),
+    ];
+    let events = "{\"ts\":150,\"type\":2,\"data\":{\"node\":7}}\n{\"ts\":250,\"type\":3,\"data\":{\"node\":8}}\n";
+    let (status, _, bytes) = request_with_headers(
+        &app.router,
+        "POST",
+        "/api/v1/recordings/events",
+        Some("application/x-ndjson"),
+        &recording_headers,
+        events,
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    assert_eq!(json_body(&bytes)?["accepted"], 2);
+
+    // Recording attribution: the harness session now owns a replay.
+    let (status, _, bytes) = request(
+        &app.router,
+        "GET",
+        "/api/v1/sessions/01JZZHUBSESSION00000000000000/recording",
+        None,
+        Body::empty(),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    let recording = json_body(&bytes)?;
+    assert_eq!(recording["hasData"], true);
+    assert_eq!(recording["tabs"].as_array().map(Vec::len), Some(1));
+
+    // Stream discovery surfaces the recorded document.
+    let (status, _, bytes) = request(
+        &app.router,
+        "GET",
+        "/api/v1/recordings/streams",
+        None,
+        Body::empty(),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    let streams = json_body(&bytes)?;
+    let stream = streams["streams"]
+        .as_array()
+        .and_then(|items| {
+            items
+                .iter()
+                .find(|item| item["documentId"] == "33D25F3CF060E81B14070BC356FF1871")
+        })
+        .cloned()
+        .expect("ingested stream is listed");
+    assert_eq!(stream["tabId"], 101);
+    assert_eq!(stream["eventCount"], 2);
+
+    // Tab-scoped event reads bypass session attribution entirely.
+    let (status, _, bytes) = request(
+        &app.router,
+        "GET",
+        "/api/v1/recordings/streams/33D25F3CF060E81B14070BC356FF1871/events",
+        None,
+        Body::empty(),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    let ndjson = String::from_utf8(bytes)?;
+    let lines: Vec<&str> = ndjson.lines().collect();
+    assert_eq!(lines.len(), 2);
+    assert!(lines[0].contains("\"ts\":150"));
+
+    // Time-window filtering narrows the read.
+    let (status, _, bytes) = request(
+        &app.router,
+        "GET",
+        "/api/v1/recordings/streams/33D25F3CF060E81B14070BC356FF1871/events?fromMs=200",
+        None,
+        Body::empty(),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    let ndjson = String::from_utf8(bytes)?;
+    assert_eq!(ndjson.lines().count(), 1);
+    assert!(ndjson.contains("\"ts\":250"));
+
+    // Release the tab and end the session; the audit trail keeps the rows.
+    let (status, _, _) = request(
+        &app.router,
+        "POST",
+        "/api/v1/harness/sessions/01JZZHUBSESSION00000000000000/tabs/101/release",
+        None,
+        Body::empty(),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+
+    let (status, _, bytes) = request(
+        &app.router,
+        "POST",
+        "/api/v1/harness/sessions/01JZZHUBSESSION00000000000000/end",
+        Some("application/json"),
+        Body::from(json!({ "kind": "normal" }).to_string()),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    assert_eq!(json_body(&bytes)?, json!({ "ended": true }));
+
+    let (status, _, bytes) = request(
+        &app.router,
+        "GET",
+        "/api/v1/sessions/01JZZHUBSESSION00000000000000",
+        None,
+        Body::empty(),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    let detail = json_body(&bytes)?;
+    assert_eq!(detail["session"]["status"], "done");
+    Ok(())
+}
+
+#[tokio::test]
+async fn harness_streams_list_filters_and_validates() -> anyhow::Result<()> {
+    let app = test_app().await?;
+
+    // Empty index is an empty list, not an error.
+    let (status, _, bytes) = request(
+        &app.router,
+        "GET",
+        "/api/v1/recordings/streams",
+        None,
+        Body::empty(),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    assert_eq!(
+        json_body(&bytes)?["streams"].as_array().map(Vec::len),
+        Some(0)
+    );
+
+    // Claim + ingest two documents on different tabs for one harness session.
+    let session_id = "01JZZHUBFILTERS000000000000000";
+    let (status, _, _) = request(
+        &app.router,
+        "POST",
+        "/api/v1/harness/sessions",
+        Some("application/json"),
+        Body::from(json!({ "sessionId": session_id }).to_string()),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    for (tab, document) in [
+        (101, "33D25F3CF060E81B14070BC356FF1871"),
+        (102, "8395FF2EF4A1D8579F1917B3B54ADECE"),
+    ] {
+        let (status, _, _) = request(
+            &app.router,
+            "POST",
+            &format!("/api/v1/harness/sessions/{session_id}/tabs"),
+            Some("application/json"),
+            Body::from(json!({ "tabId": tab, "claimedAt": 100 }).to_string()),
+        )
+        .await?;
+        assert_eq!(status, StatusCode::OK);
+        let batch_id = format!("batch-{tab}");
+        let tab_id = tab.to_string();
+        let headers = [
+            ("x-recording-tab-id", tab_id.as_str()),
+            ("x-recording-document-id", document),
+            ("x-recording-batch-id", batch_id.as_str()),
+        ];
+        let (status, _, _) = request_with_headers(
+            &app.router,
+            "POST",
+            "/api/v1/recordings/events",
+            Some("application/x-ndjson"),
+            &headers,
+            "{\"ts\":120,\"type\":2,\"data\":{}}\n{\"ts\":180,\"type\":3,\"data\":{}}\n",
+        )
+        .await?;
+        assert_eq!(status, StatusCode::OK);
+    }
+
+    // Tab filter narrows to one stream.
+    let (status, _, bytes) = request(
+        &app.router,
+        "GET",
+        "/api/v1/recordings/streams?tabId=102",
+        None,
+        Body::empty(),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    let streams = json_body(&bytes)?["streams"]
+        .as_array()
+        .cloned()
+        .unwrap_or_default();
+    assert_eq!(streams.len(), 1);
+    assert_eq!(streams[0]["tabId"], 102);
+
+    // Time window that misses every event returns nothing.
+    let (status, _, bytes) = request(
+        &app.router,
+        "GET",
+        "/api/v1/recordings/streams?fromMs=10000",
+        None,
+        Body::empty(),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    assert_eq!(
+        json_body(&bytes)?["streams"].as_array().map(Vec::len),
+        Some(0)
+    );
+
+    // Unknown document id reads as empty NDJSON (idempotent discovery).
+    let (status, _, bytes) = request(
+        &app.router,
+        "GET",
+        "/api/v1/recordings/streams/FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF/events",
+        None,
+        Body::empty(),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    assert!(String::from_utf8(bytes)?.trim().is_empty());
+
+    // End-of-session teardown releases both claims.
+    let (status, _, _) = request(
+        &app.router,
+        "POST",
+        &format!("/api/v1/harness/sessions/{session_id}/end"),
+        Some("application/json"),
+        Body::from(json!({}).to_string()),
+    )
+    .await?;
+    assert_eq!(status, StatusCode::OK);
+    Ok(())
+}
diff --git a/packages/browseros-agent/contracts/claw-api/openapi.yaml b/packages/browseros-agent/contracts/claw-api/openapi.yaml
index 984f324ea..60d657308 100644
--- a/packages/browseros-agent/contracts/claw-api/openapi.yaml
+++ b/packages/browseros-agent/contracts/claw-api/openapi.yaml
@@ -28,6 +28,20 @@ paths:
     $ref: ./paths/recordings.yaml#/events
   /api/v1/recordings/events:
     $ref: ./paths/recordings.yaml#/ingest
+  /api/v1/recordings/streams:
+    $ref: ./paths/recordings.yaml#/streamList
+  /api/v1/recordings/streams/{documentId}/events:
+    $ref: ./paths/recordings.yaml#/streamEvents
+  /api/v1/harness/sessions:
+    $ref: ./paths/harness.yaml#/sessionStart
+  /api/v1/harness/sessions/{sessionId}/end:
+    $ref: ./paths/harness.yaml#/sessionEnd
+  /api/v1/harness/sessions/{sessionId}/dispatches:
+    $ref: ./paths/harness.yaml#/dispatchReport
+  /api/v1/harness/sessions/{sessionId}/tabs:
+    $ref: ./paths/harness.yaml#/tabClaim
+  /api/v1/harness/sessions/{sessionId}/tabs/{tabId}/release:
+    $ref: ./paths/harness.yaml#/tabRelease
   /api/v1/sessions/{sessionId}/preview:
     $ref: ./paths/sessions.yaml#/preview
   /api/v1/sessions/{sessionId}/screenshots:
diff --git a/packages/browseros-agent/contracts/claw-api/paths/harness.yaml b/packages/browseros-agent/contracts/claw-api/paths/harness.yaml
new file mode 100644
index 000000000..e270ff640
--- /dev/null
+++ b/packages/browseros-agent/contracts/claw-api/paths/harness.yaml
@@ -0,0 +1,206 @@
+sessionStart:
+  post:
+    operationId: startHarnessSession
+    summary: Start an out-of-process harness session (append-only audit reporting).
+    requestBody:
+      required: true
+      content:
+        application/json:
+          schema:
+            type: object
+            additionalProperties: false
+            properties:
+              sessionId:
+                type: string
+                description: Client-supplied Ulid-shaped session id; minted when absent.
+              agentId:
+                type: string
+                default: hub
+              slug:
+                type: string
+                default: hub
+              agentLabel:
+                type: string
+                default: hub-browser
+              clientName:
+                type: string
+                default: hub
+              clientVersion:
+                type: string
+    responses:
+      '200':
+        description: The session id to reference in subsequent harness reports.
+        content:
+          application/json:
+            schema:
+              $ref: ../schemas/harness.yaml#/HarnessSessionStartResponse
+      '400':
+        $ref: ../responses.yaml#/BadRequest
+      '500':
+        $ref: ../responses.yaml#/InternalError
+
+sessionEnd:
+  post:
+    operationId: endHarnessSession
+    summary: End a harness session and release its open tab-ownership windows.
+    parameters:
+      - $ref: ../parameters.yaml#/SessionId
+    requestBody:
+      required: true
+      content:
+        application/json:
+          schema:
+            type: object
+            additionalProperties: false
+            properties:
+              kind:
+                type: string
+                default: normal
+              reason:
+                type: string
+    responses:
+      '200':
+        description: Session ended.
+        content:
+          application/json:
+            schema:
+              $ref: ../schemas/harness.yaml#/HarnessSessionEndResponse
+      '404':
+        $ref: ../responses.yaml#/NotFound
+      '500':
+        $ref: ../responses.yaml#/InternalError
+
+dispatchReport:
+  post:
+    operationId: reportHarnessDispatch
+    summary: Append one completed tool dispatch to the audit trail.
+    description: >-
+      For harnesses that drive the browser through their own connection (e.g.
+      hub over CDP) — mirrors the MCP dispatch audit row so the cockpit
+      timeline, session list, and replay attribution work identically.
+    parameters:
+      - $ref: ../parameters.yaml#/SessionId
+    requestBody:
+      required: true
+      content:
+        application/json:
+          schema:
+            type: object
+            additionalProperties: false
+            required: [toolName]
+            properties:
+              toolName:
+                type: string
+              pageId:
+                type: integer
+                format: int64
+              tabId:
+                type: integer
+                format: int64
+              targetId:
+                type: string
+              url:
+                type: string
+              title:
+                type: string
+              args:
+                type: object
+              resultMeta:
+                type: string
+                default: ok
+              durationMs:
+                type: integer
+                format: int64
+              createdAt:
+                type: integer
+                format: int64
+                description: Unix-epoch milliseconds.
+              dispatchId:
+                type: string
+              parentDispatchId:
+                type: string
+              agentId:
+                type: string
+              slug:
+                type: string
+              agentLabel:
+                type: string
+    responses:
+      '200':
+        description: Persisted row id.
+        content:
+          application/json:
+            schema:
+              $ref: ../schemas/harness.yaml#/HarnessDispatchResponse
+      '400':
+        $ref: ../responses.yaml#/BadRequest
+      '404':
+        $ref: ../responses.yaml#/NotFound
+      '500':
+        $ref: ../responses.yaml#/InternalError
+
+tabClaim:
+  post:
+    operationId: claimHarnessTab
+    summary: Open a tab-ownership window so recordings attribute to the harness session.
+    parameters:
+      - $ref: ../parameters.yaml#/SessionId
+    requestBody:
+      required: true
+      content:
+        application/json:
+          schema:
+            type: object
+            additionalProperties: false
+            required: [tabId]
+            properties:
+              tabId:
+                type: integer
+                format: int64
+              targetId:
+                type: string
+              claimedAt:
+                type: integer
+                format: int64
+                description: Unix-epoch milliseconds; defaults to now.
+              agentId:
+                type: string
+    responses:
+      '200':
+        description: Claim window opened.
+        content:
+          application/json:
+            schema:
+              $ref: ../schemas/harness.yaml#/HarnessClaimTabResponse
+      '400':
+        $ref: ../responses.yaml#/BadRequest
+      '404':
+        $ref: ../responses.yaml#/NotFound
+      '500':
+        $ref: ../responses.yaml#/InternalError
+
+tabRelease:
+  post:
+    operationId: releaseHarnessTab
+    summary: Close this session's open tab-ownership window.
+    parameters:
+      - $ref: ../parameters.yaml#/SessionId
+      - name: tabId
+        in: path
+        required: true
+        schema:
+          type: integer
+          format: int64
+    responses:
+      '200':
+        description: Claim window closed.
+        content:
+          application/json:
+            schema:
+              $ref: ../schemas/harness.yaml#/HarnessReleaseTabResponse
+      '400':
+        $ref: ../responses.yaml#/BadRequest
+      '404':
+        $ref: ../responses.yaml#/NotFound
+      '500':
+        $ref: ../responses.yaml#/InternalError
diff --git a/packages/browseros-agent/contracts/claw-api/paths/recordings.yaml b/packages/browseros-agent/contracts/claw-api/paths/recordings.yaml
index e21929257..6ab645e84 100644
--- a/packages/browseros-agent/contracts/claw-api/paths/recordings.yaml
+++ b/packages/browseros-agent/contracts/claw-api/paths/recordings.yaml
@@ -1,3 +1,72 @@
+streamList:
+  get:
+    operationId: listRecordingStreams
+    summary: Newest-first index of recorded streams for out-of-process discovery.
+    parameters:
+      - name: fromMs
+        in: query
+        schema:
+          type: integer
+          format: int64
+      - name: toMs
+        in: query
+        schema:
+          type: integer
+          format: int64
+      - name: tabId
+        in: query
+        schema:
+          type: integer
+          format: int64
+      - name: limit
+        in: query
+        schema:
+          type: integer
+          format: int64
+          default: 200
+          maximum: 1000
+    responses:
+      '200':
+        description: Recorded stream index.
+        content:
+          application/json:
+            schema:
+              $ref: ../schemas/recordings.yaml#/RecordingStreamList
+      '500':
+        $ref: ../responses.yaml#/InternalError
+
+streamEvents:
+  get:
+    operationId: readRecordingStreamEvents
+    summary: Read one document's rrweb events as NDJSON, bypassing session attribution.
+    parameters:
+      - name: documentId
+        in: path
+        required: true
+        schema:
+          type: string
+      - name: fromMs
+        in: query
+        schema:
+          type: integer
+          format: int64
+      - name: toMs
+        in: query
+        schema:
+          type: integer
+          format: int64
+    responses:
+      '200':
+        description: Newline-delimited rrweb events.
+        content:
+          application/x-ndjson:
+            schema:
+              type: string
+      '400':
+        $ref: ../responses.yaml#/BadRequest
+      '500':
+        $ref: ../responses.yaml#/InternalError
+
 ingest:
   post:
     operationId: appendRecordingEvents
diff --git a/packages/browseros-agent/contracts/claw-api/schemas/harness.yaml b/packages/browseros-agent/contracts/claw-api/schemas/harness.yaml
new file mode 100644
index 000000000..fb66b5ce5
--- /dev/null
+++ b/packages/browseros-agent/contracts/claw-api/schemas/harness.yaml
@@ -0,0 +1,42 @@
+HarnessSessionStartResponse:
+  type: object
+  additionalProperties: false
+  required: [sessionId]
+  properties:
+    sessionId:
+      type: string
+      description: The started session id (client-supplied or server-minted).
+
+HarnessSessionEndResponse:
+  type: object
+  additionalProperties: false
+  required: [ended]
+  properties:
+    ended:
+      type: boolean
+
+HarnessDispatchResponse:
+  type: object
+  additionalProperties: false
+  required: [dispatchId]
+  properties:
+    dispatchId:
+      type: integer
+      format: int64
+      description: Persisted audit row id for the reported dispatch.
+
+HarnessClaimTabResponse:
+  type: object
+  additionalProperties: false
+  required: [claimed]
+  properties:
+    claimed:
+      type: boolean
+
+HarnessReleaseTabResponse:
+  type: object
+  additionalProperties: false
+  required: [released]
+  properties:
+    released:
+      type: boolean
diff --git a/packages/browseros-agent/contracts/claw-api/schemas/recordings.yaml b/packages/browseros-agent/contracts/claw-api/schemas/recordings.yaml
index f36abbfc0..0c049658e 100644
--- a/packages/browseros-agent/contracts/claw-api/schemas/recordings.yaml
+++ b/packages/browseros-agent/contracts/claw-api/schemas/recordings.yaml
@@ -77,6 +77,45 @@ RecordingSegmentMetadata:
     legacy:
       type: boolean
       description: True when the source predates trustworthy Chrome document identity.
+RecordingStreamEntry:
+  type: object
+  additionalProperties: false
+  required: [documentId, tabId, firstEventAt, lastEventAt, sizeBytes, eventCount, hasGap]
+  properties:
+    documentId:
+      type: string
+    tabId:
+      type: integer
+      format: int64
+    targetId:
+      type: string
+    firstEventAt:
+      type: integer
+      format: int64
+      description: Unix-epoch milliseconds.
+    lastEventAt:
+      type: integer
+      format: int64
+      description: Unix-epoch milliseconds.
+    sizeBytes:
+      type: integer
+      format: int64
+    eventCount:
+      type: integer
+      format: int64
+    hasGap:
+      type: boolean
+
+RecordingStreamList:
+  type: object
+  additionalProperties: false
+  required: [streams]
+  properties:
+    streams:
+      type: array
+      items:
+        $ref: '#/components/schemas/RecordingStreamEntry'
+
 AppendRecordingEventsResponse:
   type: object
   additionalProperties: false
diff --git a/packages/browseros-agent/crates/claw-api/src/generated/harness.rs b/packages/browseros-agent/crates/claw-api/src/generated/harness.rs
new file mode 100644
index 000000000..9f04f8c3e
--- /dev/null
+++ b/packages/browseros-agent/crates/claw-api/src/generated/harness.rs
@@ -0,0 +1,71 @@
+/*
+ * BrowserClaw API
+ *
+ * Implementation-neutral REST contract shared by BrowserClaw clients and servers.
+ *
+ * The version of the OpenAPI document: 1.0.0
+ *
+ * Generated by: https://openapi-generator.tech
+ */
+
+use serde::{Deserialize, Serialize};
+
+#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
+pub struct HarnessSessionStartResponse {
+    #[serde(rename = "sessionId")]
+    pub session_id: String,
+}
+
+impl HarnessSessionStartResponse {
+    pub fn new(session_id: String) -> HarnessSessionStartResponse {
+        HarnessSessionStartResponse { session_id }
+    }
+}
+
+#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
+pub struct HarnessSessionEndResponse {
+    #[serde(rename = "ended")]
+    pub ended: bool,
+}
+
+impl HarnessSessionEndResponse {
+    pub fn new(ended: bool) -> HarnessSessionEndResponse {
+        HarnessSessionEndResponse { ended }
+    }
+}
+
+#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
+pub struct HarnessDispatchResponse {
+    #[serde(rename = "dispatchId")]
+    pub dispatch_id: i64,
+}
+
+impl HarnessDispatchResponse {
+    pub fn new(dispatch_id: i64) -> HarnessDispatchResponse {
+        HarnessDispatchResponse { dispatch_id }
+    }
+}
+
+#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
+pub struct HarnessClaimTabResponse {
+    #[serde(rename = "claimed")]
+    pub claimed: bool,
+}
+
+impl HarnessClaimTabResponse {
+    pub fn new(claimed: bool) -> HarnessClaimTabResponse {
+        HarnessClaimTabResponse { claimed }
+    }
+}
+
+#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
+pub struct HarnessReleaseTabResponse {
+    #[serde(rename = "released")]
+    pub released: bool,
+}
+
+impl HarnessReleaseTabResponse {
+    pub fn new(released: bool) -> HarnessReleaseTabResponse {
+        HarnessReleaseTabResponse { released }
+    }
+}
diff --git a/packages/browseros-agent/crates/claw-api/src/generated/mod.rs b/packages/browseros-agent/crates/claw-api/src/generated/mod.rs
index 646a178ba..e71cf5feb 100644
--- a/packages/browseros-agent/crates/claw-api/src/generated/mod.rs
+++ b/packages/browseros-agent/crates/claw-api/src/generated/mod.rs
@@ -8,7 +8,9 @@ pub mod connections;
 pub use self::connections::*;
 pub mod dispatches;
 pub use self::dispatches::*;
+pub mod harness;
 pub mod recordings;
+pub use self::harness::*;
 pub use self::recordings::*;
 pub mod sessions;
 pub use self::sessions::*;
diff --git a/packages/browseros-agent/crates/claw-api/src/generated/recordings.rs b/packages/browseros-agent/crates/claw-api/src/generated/recordings.rs
index a903fdf5e..218d9011b 100644
--- a/packages/browseros-agent/crates/claw-api/src/generated/recordings.rs
+++ b/packages/browseros-agent/crates/claw-api/src/generated/recordings.rs
@@ -11,6 +11,61 @@
 use crate::models;
 use serde::{Deserialize, Serialize};
 
+#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
+pub struct RecordingStreamEntry {
+    #[serde(rename = "documentId")]
+    pub document_id: String,
+    #[serde(rename = "tabId")]
+    pub tab_id: i64,
+    #[serde(rename = "targetId", skip_serializing_if = "Option::is_none")]
+    pub target_id: Option<String>,
+    #[serde(rename = "firstEventAt")]
+    pub first_event_at: i64,
+    #[serde(rename = "lastEventAt")]
+    pub last_event_at: i64,
+    #[serde(rename = "sizeBytes")]
+    pub size_bytes: i64,
+    #[serde(rename = "eventCount")]
+    pub event_count: i64,
+    #[serde(rename = "hasGap")]
+    pub has_gap: bool,
+}
+
+impl RecordingStreamEntry {
+    pub fn new(
+        document_id: String,
+        tab_id: i64,
+        first_event_at: i64,
+        last_event_at: i64,
+        size_bytes: i64,
+        event_count: i64,
+        has_gap: bool,
+    ) -> RecordingStreamEntry {
+        RecordingStreamEntry {
+            document_id,
+            tab_id,
+            target_id: None,
+            first_event_at,
+            last_event_at,
+            size_bytes,
+            event_count,
+            has_gap,
+        }
+    }
+}
+
+#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
+pub struct RecordingStreamList {
+    #[serde(rename = "streams")]
+    pub streams: Vec<RecordingStreamEntry>,
+}
+
+impl RecordingStreamList {
+    pub fn new(streams: Vec<RecordingStreamEntry>) -> RecordingStreamList {
+        RecordingStreamList { streams }
+    }
+}
+
 #[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
 pub struct AppendRecordingEventsResponse {
     #[serde(rename = "accepted")]
